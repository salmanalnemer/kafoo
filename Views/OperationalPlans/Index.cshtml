@model IReadOnlyList<Kafo.Web.Models.OperationalPlanDocument>

@{
    ViewData["Title"] = "الخطط التشغيلية";
}

<section class="operational-public-page">
    <div class="home-container">

        <div class="operational-public-head">
            <span>ملفات الحوكمة</span>
            <h1>الخطط التشغيلية</h1>
            <p>استعراض وتحميل الخطط التشغيلية المعتمدة للجمعية.</p>
        </div>

        <div class="operational-table-card">
            <div class="operational-table-wrap">
                <table class="operational-public-table">
                    <thead>
                        <tr>
                            <th>العنوان</th>
                            <th>السنة</th>
                            <th>التصنيف</th>
                            <th>تحميل</th>
                        </tr>
                    </thead>

                    <tbody>
                        @if (Model == null || !Model.Any())
                        {
                            <tr>
                                <td colspan="4" class="operational-empty-row">
                                    لا توجد ملفات حالياً.
                                </td>
                            </tr>
                        }
                        else
                        {
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td class="operational-title-cell">
                                        <span class="pdf-mark">PDF</span>
                                        <div>
                                            <strong>@item.Title</strong>
                                            @if (!string.IsNullOrWhiteSpace(item.Description))
                                            {
                                                <small>@item.Description</small>
                                            }
                                        </div>
                                    </td>

                                    <td>@item.Year</td>

                                    <td>
                                        <span class="operational-category">@item.Category</span>
                                    </td>

                                    <td>
                                        @if (!string.IsNullOrWhiteSpace(item.FilePath))
                                        {
                                            <a class="operational-download" href="@item.FilePath" target="_blank" rel="noopener noreferrer" download>
                                                تحميل
                                            </a>
                                        }
                                        else
                                        {
                                            <span>-</span>
                                        }
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>

    </div>
</section>
