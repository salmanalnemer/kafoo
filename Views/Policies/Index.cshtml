@model IReadOnlyList<Kafo.Web.Models.PolicyDocument>

@{
    ViewData["Title"] = "السياسات واللوائح";
}

<section class="policies-public-page">
    <div class="home-container">

        <div class="policies-public-head">
            <span>ملفات الحوكمة</span>
            <h1>السياسات واللوائح</h1>
            <p>استعراض وتحميل ملفات السياسات واللوائح المعتمدة للجمعية.</p>
        </div>

        <div class="policies-table-card">
            <div class="policies-table-wrap">
                <table class="policies-public-table">
                    <thead>
                        <tr>
                            <th>العنوان</th>
                            <th>السنة</th>
                            <th>التصنيف</th>
                            <th>تحميل</th>
                        </tr>
                    </thead>

                    <tbody>
                        @if (Model == null || !Model.Any())
                        {
                            <tr>
                                <td colspan="4" class="policies-empty-row">
                                    لا توجد ملفات حالياً.
                                </td>
                            </tr>
                        }
                        else
                        {
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td class="policies-title-cell">
                                        <span class="pdf-mark">PDF</span>
                                        <div>
                                            <strong>@item.Title</strong>
                                            @if (!string.IsNullOrWhiteSpace(item.Description))
                                            {
                                                <small>@item.Description</small>
                                            }
                                        </div>
                                    </td>

                                    <td>@item.Year</td>
                                    <td>
                                        <span class="policies-category">@item.Category</span>
                                    </td>

                                    <td>
                                        @if (!string.IsNullOrWhiteSpace(item.FilePath))
                                        {
                                            <a class="policies-download" href="@item.FilePath" target="_blank" rel="noopener noreferrer" download>
                                                تحميل
                                            </a>
                                        }
                                        else
                                        {
                                            <span>-</span>
                                        }
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>

    </div>
</section>
