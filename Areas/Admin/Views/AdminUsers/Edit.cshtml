@model Kafo.Web.ViewModels.Admin.AdminUserFormViewModel
@using Kafo.Web.Security

@{
    ViewData["Title"] = "تعديل مستخدم لوحة التحكم";
    var actorRoleCode = ViewBag.ActorRoleCode as string ?? AdminRolePolicy.Supervisor;
    var canAssignManagers = actorRoleCode == AdminRolePolicy.SystemManager;
}

<div class="adm-users-page">
    <div class="adm-users-hero">
        <div>
            <span>مستخدمي لوحة التحكم</span>
            <h1>تعديل المستخدم</h1>
            <p>تحديث البريد والبيانات والدور وحالة الحساب. كلمة المرور تُعاد من زر الإرسال في قائمة المستخدمين.</p>
        </div>

        <div class="adm-users-actions">
            <a href="/Admin/Users">رجوع</a>
        </div>
    </div>

    <div class="adm-form-card">
        <form method="post" action="/Admin/Users/Edit/@Model.Id" class="adm-form">
            @Html.AntiForgeryToken()
            <input asp-for="Id" type="hidden" />

            <div asp-validation-summary="ModelOnly" class="validation-summary"></div>

            <div class="adm-form-grid">
                <div class="form-group">
                    <label>الاسم الكامل</label>
                    <input asp-for="FullName" />
                    <span asp-validation-for="FullName"></span>
                </div>

                <div class="form-group">
                    <label>اسم المستخدم الداخلي</label>
                    <input asp-for="UserName" dir="ltr" />
                    <span asp-validation-for="UserName"></span>
                </div>

                <div class="form-group">
                    <label>البريد الإلكتروني للدخول وOTP</label>
                    <input asp-for="Email" type="email" dir="ltr" />
                    <span asp-validation-for="Email"></span>
                </div>

                <div class="form-group">
                    <label>رقم الجوال</label>
                    <input asp-for="Phone" dir="ltr" inputmode="tel" />
                    <span asp-validation-for="Phone"></span>
                </div>

                <div class="form-group full">
                    <label>نوع المستخدم</label>
                    <select asp-for="RoleCode">
                        @if (canAssignManagers)
                        {
                            <option value="@AdminRolePolicy.SystemManager">مدير النظام</option>
                            <option value="@AdminRolePolicy.AdministrationManager">مدير الإدارة</option>
                        }
                        <option value="@AdminRolePolicy.Supervisor">مشرف</option>
                    </select>
                    <span asp-validation-for="RoleCode"></span>
                </div>

                <label class="adm-check">
                    <input asp-for="IsActive" />
                    <span>الحساب مفعل</span>
                </label>
            </div>

            <div class="adm-form-actions">
                <button type="submit">حفظ التعديلات</button>
            </div>
        </form>
    </div>
</div>
